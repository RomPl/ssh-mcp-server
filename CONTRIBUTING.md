# Вклад в проект

Требования:
- не коммитить секреты (`.env`, приватные ключи)
- не добавлять backup‑файлы в git (`*.bak_*`)
- изменения должны быть минимальными и проверяемыми

Перед PR:
- проверить синтаксис: `python -m py_compile *.py`
- проверить запуск: `server_http.py` и `server_stdio.py`
